import { CPButton } from "@components/library";
import { Button } from "@cube-dev/ui-kit";
import React from "react";

function Index() {
  return (
    <div>
      <CPButton>button</CPButton>
    </div>
  );
}

export default Index;
